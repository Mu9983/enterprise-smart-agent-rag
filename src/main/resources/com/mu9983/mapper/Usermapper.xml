<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mu9983.mapper.UserMapper">

    <select id="selectByUsernameAndPassword" resultType="com.mu9983.entity.User">
        select id, username, password from user where username = #{username} and password = #{password}
    </select>

    <select id="selectById" resultType="com.mu9983.entity.User">
        select id, username, gender, age, email, address, permission, avatar from user where id = #{id}
    </select>

    <update id="updateUser">
        update user set username = #{username}, email = #{email},
                        address = #{address}, avatar = #{avatar} where id = #{id}
    </update>

    <select id="selectVerifyUserPassword" resultType="com.mu9983.entity.User">
        select id, username, password from user where username = #{username} and password = #{password}
    </select>
</mapper>