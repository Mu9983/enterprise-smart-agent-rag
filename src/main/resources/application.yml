spring:
  application:
    name: enterprise-smart-agent-rag
  datasource:
    url: jdbc:mysql://localhost:3306/esar?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 807806
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.75.128
      port: 6379
      password: 807806
      database: 0
  servlet:
    multipart:
      # 单个文件最大
      max-file-size: 10MB
      # 单次请求总文件大小
      max-request-size: 50MB