spring:
  application:
    name: enterprise-smart-agent-rag
  datasource:
    url: jdbc:mysql://localhost:3306/esar?useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 807806
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.75.128
      port: 6379
      password: 807806
      database: 0
  servlet:
    multipart:
      # 单个文件最大
      max-file-size: 10MB
      # 单次请求总文件大小
      max-request-size: 50MB
mybatis:
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 8080
  address: 0.0.0.0